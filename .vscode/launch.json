{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run yarn start:debug",
            "type": "node-terminal",
            "request": "launch",
            "command": "yarn run start:debug",
            "skipFiles": [
                "<node_internals>/**",
                "${workspaceFolder}/node_modules/**"
            ]
        }
    ]
}